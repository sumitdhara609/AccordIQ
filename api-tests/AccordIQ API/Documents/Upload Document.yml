info:
  name: Upload Document
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/api/v1/documents/search?keyword=test&status=UPLOADED
  params:
    - name: keyword
      value: test
      type: query
    - name: status
      value: UPLOADED
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
