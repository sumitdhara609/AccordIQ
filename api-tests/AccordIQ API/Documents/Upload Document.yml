info:
  name: Upload Document
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/api/v1/analyze/text
  body:
    type: json
    data: |-
      {
        "text": "This agreement automatically renews every year unless cancelled with 30 days notice."
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
